The directory contains both C and Python programs to solve the following questions

1. A Shell script that runs a Python script.

The Python file name will be saved in the environment variable $PYFILE
